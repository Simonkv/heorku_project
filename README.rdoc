How to create a pull request:

1. heroku login

Create branch

1. git checkout -b my-branch
2. git add .
3. git commit -m " "
4. git push origin my-branch
5. Create pull request at github

Delete branch
1. git branch -d my-branch

Delete branch
1. git branch -d my-branch



str.gsub(/\s+/m, ' ').strip.split(" ")

returns 
["aa", "bbb", "cc", "dd", "ee"]